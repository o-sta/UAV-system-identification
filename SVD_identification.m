clear
close all

% sim('SVD.slx');

%--------------------------------------------------------------------------
% ● パラメータ生成
%--------------------------------------------------------------------------
num = [1];
den = [1 3];
cP = tf(num,den)
dP = c2d(cP,0.1)

[vec t] = impulse(dP,5);

stem(t,vec);

%--------------------------------------------------------------------------
% ● インパルス応答からハンケル行列を生成(i行j列)
% i : vec
% o : H
%--------------------------------------------------------------------------
% パラメータ
i = 20;
% ベクトルを分解
c = vec(1:i)
r = vec(i:numel(vec))
% ハンケル行列作成
H = hankel(c,r)

%--------------------------------------------------------------------------
% ● 特異値分解SVD
%--------------------------------------------------------------------------
[U S V] = svd(H);

%--------------------------------------------------------------------------
% ● 不要な成分を取り出す
%--------------------------------------------------------------------------














